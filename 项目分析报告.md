# 项目分析报告：PikPak Rename Pro

## 1. 项目概述
这是一个浏览器油猴脚本（Tampermonkey/Violentmonkey），为 PikPak 云盘网页版提供悬浮面板，支持批量重命名、自动分类、查重、字幕匹配、剧集标准化、离线任务监控、批量分享等功能。单文件约 2275 行，全部原生 JavaScript，无外部构建工具。

---

## 2. 发现的 Bug / 逻辑缺陷（需修复）

### 🔴 Bug 1：媒体子标签切换事件绑定错误（功能完全失效）
- **位置：**`pikpak-rename.user.js` 第 1974–1987 行
- **问题：**HTML 中按钮的属性是 `data-msub="subs"`，但 JavaScript 里读取的是 `btn.dataset.msubl`（多了字母 `l`），导致 `tab` 永远是 `undefined`，`subs` 和 `episode` 切换完全失灵。
- **修复：**将所有 `btn.dataset.msubl` 改为 `btn.dataset.msub`。

### 🟠 Bug 2：`makeDraggable` 中的全局事件监听器泄漏
- **位置：**第 500–514 行
- **问题：**`rebuildPanel()`（切换语言时触发）会移除旧 DOM 重建面板，但 `makeDraggable` 里的 `document.addEventListener('mousemove', ...)` 和 `mouseup` 是绑定在 `document` 上的，**永远不会被移除**。每次切换语言都会多累积一组监听器，导致页面拖动卡顿、内存泄漏。
- **修复：**把 `mousemove`/`mouseup` 的监听器改为按需添加/移除，或用 `AbortController` / `removeEventListener` 在面板销毁时清理。

### 🟡 Bug 3：多个功能模块共用了同一个 `t('delay')` 翻译键，导致 UI 显示错误
- **位置：**
  - 第 622 行（文件计数）：`${t('delay')}: ${filtered.length}` → 显示为 "**延迟**: 5"，语义错误，应该是 "**匹配**: 5" 或 "**已筛选**: 5"。
  - 第 1881 行（分享有效期）：`<option value="7" selected>7${t('delay')}</option>` → 显示为 "**7Delay**"（英文）或 "**7延迟**"（中文），语义应为 "**7天**"。
- **修复：**新增独立的翻译键，如 `filteredCount`、`days`，不要在不同语义下复用 `delay`。

### 🟢 Bug 4：`document.execCommand('copy')` 已弃用
- **位置：**第 2238 行（导出预设功能）
- **问题：**现代浏览器逐步淘汰此 API，未来可能失效。且在某些安全策略下（HTTPS 权限不足）会静默失败。
- **修复：**改为 `navigator.clipboard.writeText(presets)` 并处理 Promise 的 catch 错误。

---

## 3. 用户体验与架构问题

### Issue 5：面板初次出现位置固定，可能遮挡页面内容
- **表现：**`top: 80px; left: 20px;` 硬编码左上角。用户每次刷新都要手动挪开。
- **建议：**将位置存入 `localStorage`，下次打开时读取上次拖拽后的坐标。

### Issue 6：小白用户门槛过高 —— 没有任何示例和引导
- **表现：**
  - README 中提到 "regex find-and-replace"，但普通用户完全不懂正则。
  - UI 中 "Credential Status: Missing: Token / DeviceId / CaptchaToken" 让新手一脸茫然。
- **建议：**
  - 预设几个常用重命名规则模板（如 "去掉空格"、"统一加序号"、"替换扩展名"）。
  - 凭证状态区域增加一句用户提示："请在 PikPak 页面内任意点击一个文件夹或文件，直到状态变为 ✅ Ready"。

### Issue 7：单文件过大，维护困难
- **表现：**2275 行的单一文件，包含 6 大功能模块的 UI + 逻辑 + API 调用。
- **建议：**虽然 userscript 必须最终是单文件，但开发时可以按模块拆分为多个 `.js` 文件，再用简单的构建脚本（或 Tampermonkey 的 `// @require`）合并。至少可以在源码中用 `// ===== Module X =====` 大区块清晰分隔。

### Issue 8：所有 CSS 为内联样式，无法热更新或主题覆盖
- **表现：**全部通过 `element.style.cssText = '...'` 硬编码颜色、边距。
- **建议：**在 `createUI` 开头注入一个 `<style>` 标签统一定义 CSS 变量和类，面板 DOM 引用类名。即便 userscript 也要考虑 Dark Mode 适配。

### Issue 9：预览列表 `maxShow = 200` 限制缺乏交互反馈
- **表现：**当修改超过 200 个文件时，只显示前 200 条，底部出现 "...其余文件"。用户可能误以为只有这些会被修改。
- **建议：**在预览区域顶部加醒目提示："已为您筛选出 ${changed} 个文件需要重命名，下方仅展示前 200 条，**全部都会生效**"。

### Issue 10：正则表达式存在潜在 ReDoS 风险
- **表现：**`processName()` 中直接使用用户输入的 `new RegExp(step.search, 'g')` 对文件名执行 `replace`。若用户输入了 `(a+)+$` 这类灾难性回溯模式，会导致整个浏览器标签页卡死。
- **建议：**可选用安全的正则库，或增加超时机制（如使用 `setTimeout` 包装执行，超过 500ms 强制中断并提示 "正则表达式过于复杂"）。

### Issue 11：`localStorage` 没有版本迁移机制
- **表现：**脚本升级后如果存储的数据结构变化（如新增字段、重命名 key），旧数据直接读取可能导致 `JSON.parse` 报错或功能异常。
- **建议：**给所有 `localStorage` 存储数据加上版本号字段，启动时检查版本并做数据迁移/清理。

---

## 4. 安全性评估

- **Token 拦截：**脚本通过 Hook `fetch/XMLHttpRequest` 自动捕获 PikPak 的 Bearer Token。这是该脚本的核心工作方式，**逻辑上是安全的**（数据不会外传），但用户需要信任此脚本作者。建议在 README 中明确声明这一点以消除顾虑。
- **分享模块 API：**`batchCreateShares` 限制 20 个文件，有基本风控意识，不错。

---

## 5. 修复优先级建议

| 优先级 | 问题 | 影响范围 |
|--------|------|----------|
| P0 | `btn.dataset.msubl` 拼写错误 | 媒体模块（字幕/剧集）标签完全无法切换 |
| P0 | `document` 事件监听器泄漏 | 切换语言越用越卡，内存泄漏 |
| P1 | `t('delay')` 语义复用错误 | 多处界面文案显示混乱 |
| P1 | 小白引导缺失 | 直接影响用户留存和口碑 |
| P2 | `execCommand('copy')` 弃用 | 未来浏览器兼容性 |
| P2 | 位置持久化 | 高频交互体验优化 |
| P3 | ReDoS / 数据迁移 / CSS 架构 | 长期健壮性优化 |

---

## 6. 给作者的额外建议

1. **增加 "新手模式" 开关：**默认收起 "正则"、"筛选" 等高级面板，只展示最常用的 "查找替换"、"加序号"。
2. **动画/loading 优化：**大文件夹扫描时，可以显示一个进度百分比，而不是只有一个 `Scanning...`。
3. **国际化完善：**有一些提示语（如 `log('Scan a folder first...')`）是硬编码英文，没有通过 `t()` 函数翻译，中文用户会跳出英文提示。
4. **测试清单：**如果你想稳健地迭代这个项目，建议建立一个简单的手动测试清单（每次发版前扫一遍），涵盖：扫描、预览、执行重命名、撤销、分类、去重、切换语言、切换媒体子标签、分享生成等。
  ┌───────────────────┬─────────────────────────────────────┐
  │     放置位置      │            建议截图内容             │
  ├───────────────────┼─────────────────────────────────────┤
  │ 安装脚本后        │ Tampermonkey "Install" 界面         │
  ├───────────────────┼─────────────────────────────────────┤
  │ 打开 PikPak 后    │ 脚本面板出现在网页左上角            │
  ├───────────────────┼─────────────────────────────────────┤
  │ 扫描完成后        │ "已加载 xx 个文件" + Ready 状态徽章 │
  ├───────────────────┼─────────────────────────────────────┤
  │ 查找替换区        │ 查找/替换输入框 + 添加步骤按钮      │
  ├───────────────────┼─────────────────────────────────────┤
  │ 命名选项区        │ 前缀/编号勾选框/保留扩展名          │
  ├───────────────────┼─────────────────────────────────────┤
  │ 筛选区            │ 三个筛选输入框                      │
  ├───────────────────┼─────────────────────────────────────┤
  │ 预览区            │ 旧名 → 新名 对照列表                │
  ├───────────────────┼─────────────────────────────────────┤
  │ 执行完成后        │ 日志区显示成功/失败数量             │
  ├───────────────────┼─────────────────────────────────────┤
  │ 历史/撤销         │ 历史记录列表 + 撤销按钮             │
  ├───────────────────┼─────────────────────────────────────┤
  │ 四个主标签        │ 重命名 / 整理 / 任务 / 分享 并排    │
  ├───────────────────┼─────────────────────────────────────┤
  │ Tampermonkey 面板 │ 脚本开关 ON/OFF 状态                │
  ├───────────────────┼─────────────────────────────────────┤
  │ 预设区            │ 保存/加载/导出导入界面              │
  └───────────────────┴──────────────────────────────────